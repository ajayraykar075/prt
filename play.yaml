---
 - name: install apache
   hosts: s1
   become: yes
   become_user: ubuntu
   tasks:
     - name: install apache
       apt: name=apache2 state=present

 - name: install nginx
   hosts: s2
   become: yes
   become_user: ubuntu
   tasks:
     - name: install nginx
       apt: name=nginx state=present


